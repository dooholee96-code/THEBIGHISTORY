# 빅 히스토리 연표 (Big History Timeline)

왕조·국가사와 주제사를 **하나의 표에서 나란히 비교**하는 파스텔 동화풍 역사 연표입니다.
서버도 데이터베이스도 없는 정적 웹앱이라 인터넷 없이도 동작하고,
PWA(홈 화면에 추가)나 Capacitor(iOS/Android)로 그대로 확장할 수 있습니다.

- **시대의 색** — 선사·고대·중세·근세·근대·현대. 스크롤하면 배경이 그 시대의 색으로 서서히 바뀌고,
  좌우 여백에는 시대 삽화가 은은하게 깔립니다. 낮/야경 전환도 됩니다.
- **진짜 표** — 왼쪽 세로축이 연도, 오른쪽은 카테고리마다 한 열.
  그 시대에 **사건이 있는 연도만** 행이 되고, **사건이 있는 카테고리만** 열이 됩니다.
- **같은 해에 여러 사건** — 셀에는 두 건까지 보이고, 나머지는 `N건 모두 보기` 칩을 눌러
  **월·일 목록 팝업**에서 봅니다. 항목을 누르면 아래에서 상세 시트가 올라옵니다.
- **네 개의 탭** — 연표 · 카테고리 · 검색 · 정보

현재 데이터: **11개 카테고리 / 395개 항목** (모두 `status: draft` — 검토 전 초안입니다)

> ⚠️ **내용 신뢰도**: 395개 항목은 **전부 검토 전 초안이고 출처가 확인되지 않았습니다.**
> [4장 내용의 출처와 신뢰도](#4-내용의-출처와-신뢰도) 를 꼭 읽어 주세요.

## 1. 실행하기

### 가장 간단한 방법 — 파일로 바로 열기

`index.html` 을 브라우저로 열면 끝입니다. (`data/bundle.js` 를 대신 읽습니다. 아래 참고)

### 로컬 서버로 열기 (권장)

```bash
npm run serve          # npx serve -l 5173 .  → http://localhost:5173
# 또는 Node 없이
python3 -m http.server 5173
```

> **왜 두 가지인가요?**
> 브라우저는 `file://` 로 연 페이지가 `fetch()` 로 옆의 JSON 파일을 읽는 것을 막습니다.
> 그래서 JSON 원본을 그대로 담은 사본 `data/bundle.js` 를 함께 두고, `file://` 일 때는
> 자동으로 그쪽을 읽습니다. **JSON 을 고쳤다면 `npm run build` 를 한 번 실행**해서
> 사본을 갱신해 주세요. (로컬 서버로만 쓸 거라면 없어도 됩니다.)

### 데이터 검증 / 배포 준비

```bash
npm run validate       # 스키마 + 교차 검사 (오류가 있으면 종료 코드 1)
npm run fix            # era_note 같은 자동 채움 가능한 항목을 채운 뒤 재검증
npm run build          # validate + 버전 스탬프 + data/bundle.js 재생성
```

**코드나 데이터를 고쳤으면 배포 전에 반드시 `npm run build` 를 돌리세요.**
`scripts/stamp.js` 가 css/js 참조에 `?v=<해시>` 를 붙이고 `sw.js` 의 캐시 이름을
같은 해시로 맞춥니다. 이게 없으면 브라우저·서비스워커가 예전 파일을 계속 써서
**배포해도 화면이 그대로인** 문제가 생깁니다. (해시는 소스 내용에서 자동 계산)

의존성 설치(`npm install`)는 필요 없습니다. 검증기는 Node 기본 기능만 씁니다.

---

## 2. 폴더 구조

```
index.html                  화면 뼈대 (4개 탭 + 상세 시트 + 월·일 팝업)
css/styles.css              디자인 토큰과 스타일 (시대 팔레트는 데이터에 있음)
js/data.js                  데이터 로딩 (fetch ↔ bundle 자동 전환)
js/chapters.js              시대 섹션 + 표 렌더러 (행·열·셀·접기 규칙)
js/app.js                   탭·배경 전환·카테고리·검색·시트·팝업 조립

data/index.json             ★ 매니페스트 — 데이터 파일 + 시대 구간 + 팔레트
data/<이름>.json            ★ 실제 항목들 (국가별·주제별로 파일 분리)
data/bundle.js              위 JSON 들의 자동 생성 사본 (file:// 전용, 직접 수정 금지)

schema/index.schema.json    매니페스트 JSON Schema
schema/entries.schema.json  항목 JSON Schema
scripts/validate.js         검증 스크립트
scripts/bundle.js           bundle.js 생성기
scripts/stamp.js            배포 버전 스탬프(캐시 무효화) 찍기

manifest.webmanifest, sw.js, icons/   PWA 관련 파일

LICENSE                     앱 코드 라이선스 (MIT)
THIRD-PARTY-NOTICES.md      제3자 구성요소 고지 (글꼴)
data/LICENSE.md             연표 데이터 라이선스(CC BY 4.0) + 데이터 작성 규칙
```

**화면 코드는 데이터 구조를 하드코딩하지 않습니다.** 카테고리 목록, 레인 이름, 색상,
시대 프리셋까지 전부 `data/index.json` 에서 흘러나옵니다.

---

## 3. 데이터 다루기

### 3-1. 항목 하나 추가/수정/삭제

해당 파일(`data/korea-dynasty.json` 등)의 `entries` 배열만 고치면 됩니다.

```json
{
  "id": "korea-gojoseon",
  "category": "왕조사",
  "subcategory": "한국",
  "title": "고조선",
  "region": "동아시아",
  "start_year": -2333,
  "end_year": -108,
  "era_note": "기원전 2333년 ~ 기원전 108년",
  "description": "단군왕검 건국 전승에서 비롯된 한반도 최초의 국가로, 기원전 108년 한 무제의 침공으로 멸망했다.",
  "tags": ["고대", "국가형성"],
  "sources": ["국사편찬위원회 한국사 개설"],
  "status": "draft"
}
```

| 필드 | 규칙 |
|---|---|
| `id` | 전체 데이터에서 유일. 소문자·숫자·하이픈. 관례: `<데이터셋id>-<식별자>` |
| `category` / `subcategory` | 매니페스트에 등록한 값과 **정확히** 같아야 함 (검증됨) |
| `start_year` / `end_year` | **기원전은 음수** (기원전 2333년 → `-2333`). 연도 0은 쓰지 않음 |
| | 단일 사건은 `start_year` 와 `end_year` 를 같게 → 연표에 점(point)으로 표시 |
| `era_note` | 비워두면 `npm run fix` 가 `"기원전 2333년 ~ 기원전 108년"` 형식으로 채움 |
| `summary` | (선택) 검색 결과와 월·일 팝업에 쓰이는 한 줄 요약, 60자 이내. 없으면 `description` 첫 문장을 줄여서 자동 생성 |
| `start_md` | (선택) 시작일의 월·일 `"MM-DD"`. 같은 해·같은 카테고리에 여러 사건이 있을 때 `7.28` 처럼 표시되고 정렬 기준이 된다. **확인된 날짜만** 적고, 없으면 팝업에 `연중` 으로 나온다 |
| `art` | (선택) 이 항목에 넣을 삽화의 의도. 상세 시트에 자리표시자로 표시된다 |
| `dating_note` | (선택) 연대의 근거·한계. 문헌 전승 연대, 추정치, 학설이 갈리는 연대면 여기에 밝힌다. 팝업에 눈에 띄게 표시된다 |
| `sources` | **직접 확인한 자료만** 적는다(URL 권장). 비어 있으면 팝업에 '출처 미확인' 으로 표시된다 |
| `status` | `draft`(검토 전) 또는 `reviewed`(검토 완료). draft 는 점선 테두리로 표시 |

수정 후:

```bash
npm run build     # 검증 + bundle.js 갱신
```

> **Claude Code 에게 부탁할 때**
> "조선 왕조 추가해줘, 1392~1897" 처럼 한 문장이면 됩니다.
> 해당 JSON 편집 → (필요하면) 매니페스트 갱신 → `npm run build` 까지 한 번에 처리됩니다.
> 별도 관리자 화면은 없습니다. 변경 이력은 git 로그가 검토 로그 역할을 합니다.

### 3-2. 새 국가 추가하기 (예: 베트남사)

**① 파일 만들기** — `data/vietnam-dynasty.json`

```json
{
  "$schema": "../schema/entries.schema.json",
  "dataset": "vietnam",
  "entries": [
    {
      "id": "vietnam-ly",
      "category": "왕조사",
      "subcategory": "베트남",
      "title": "리 왕조",
      "region": "동남아시아",
      "start_year": 1009,
      "end_year": 1225,
      "description": "탕롱(하노이)을 수도로 삼아 베트남의 독립 왕조 체제를 다졌다.",
      "tags": ["중세", "왕조"],
      "sources": ["위키백과 베트남사 개괄"],
      "status": "draft"
    }
  ]
}
```

**② 매니페스트에 한 줄 등록** — `data/index.json` 의 `datasets` 배열에 추가

```json
{
  "id": "vietnam",
  "file": "vietnam-dynasty.json",
  "track": "nation",
  "category": "왕조사",
  "subcategory": "베트남",
  "label": "베트남사",
  "color": "#c0392b",
  "region": "동남아시아",
  "order": 33
}
```

끝입니다. **화면 코드는 한 줄도 건드리지 않습니다.** 레인·필터 칩·색상이 자동으로 생깁니다.

### 3-3. 새 카테고리(주제) 추가하기 (예: 의학사)

국가 추가와 똑같습니다. `category` 를 `"의학사"`, `track` 을 `"theme"` 로 두면 주제사 그룹에 붙습니다.
아예 새로운 트랙(예: `"경제사 묶음"`)을 만들고 싶다면 `data/index.json` 의 `tracks` 에
`{"id": "economy", "label": "경제·사회사", "order": 3}` 을 추가하고 데이터셋의 `track` 에 그 id를 쓰면 됩니다.

### 3-4. 매니페스트 필드 요약

| 필드 | 설명 |
|---|---|
| `tracks[]` | 화면 위쪽 필터의 큰 묶음 (`nation`=왕조·국가사, `theme`=주제사) |
| `datasets[]` | 데이터 파일 목록. `id·file·track·category·subcategory·label·color` 필수 |
| `datasets[].order` | 레인 정렬 순서 (작을수록 위) |
| `datasets[].hidden_by_default` | `true` 면 처음에 꺼진 상태로 시작 |
| `eras[]` | **시대 구간**. 화면의 시대 칩·섹션·배경 팔레트가 전부 여기서 만들어진다 |
| `eras[].start_year` / `end_year` | 그 시대에 들어갈 사건의 `start_year` 범위 (끝값은 포함하지 않음) |
| `eras[].day` / `night` | 배경 그라데이션 두 색 (위→아래). 낮/야경 각각 |
| `eras[].day_blob` / `night_blob` | 배경에 떠다니는 블롭 두 색 |
| `eras[].art` | 그 시대에 넣을 삽화의 의도. 넓은 화면의 여백에 자리표시자로 표시된다 |

---

## 4. 내용의 출처와 신뢰도

**이 연표의 내용은 아직 검증되지 않았습니다.** 지금 상태를 있는 그대로 적습니다.

- 395개 항목이 **전부 `status: draft`** 이고, **출처가 확인된 항목은 0개**입니다.
- 항목의 연도와 설명은 **개설 수준의 통설을 요약한 초안**입니다. 특정 자료를 펼쳐 놓고
  대조한 것이 아니므로, 세부 연도나 표현에 오류가 있을 수 있습니다.
- 초기 버전에는 각 항목에 `"Encyclopaedia Britannica 개괄"` 같은 출처 문자열이 붙어 있었지만,
  이는 **항목별로 확인한 인용이 아니라 파일 단위로 일괄 부여된 라벨**이었습니다.
  검증된 것처럼 오해될 수 있어 **모두 비웠습니다.** 지금은 팝업에 '출처 미확인' 으로 정직하게 표시됩니다.
- 연대가 **문헌 전승·추정·학설 대립**인 항목 35개에는 `dating_note` 를 달아 근거와 한계를 밝혔습니다.
  (고조선·삼국 건국 연대, 하상주단대공정 연대, 석가모니·예수 생몰, 메소포타미아 연대기,
  선사 시대 구간 등)

### 검토해서 신뢰도를 올리는 방법

1. 항목의 연도·내용을 **직접 자료로 확인**합니다.
2. `sources` 에 확인한 자료를 적습니다. 되짚을 수 있는 형태가 좋습니다.
   ```json
   "sources": [
     "https://ko.wikipedia.org/wiki/고조선",
     "국사편찬위원회, 『한국사』 4권, 12쪽"
   ]
   ```
3. `status` 를 `reviewed` 로 바꿉니다.
4. `npm run validate` — **`reviewed` 인데 `sources` 가 비어 있으면 오류로 막습니다.**
   검증 결과 맨 끝에 `출처 표기: n/395` 로 진행 상황이 나옵니다.

git 커밋 이력이 그대로 검토 로그가 되므로, 누가 언제 무엇을 확인했는지 남습니다.

## 5. 화면 사용법

### 연표 탭

| 동작 | 방법 |
|---|---|
| 시대 이동 | 위쪽 `선사 · 고대 · 중세 · 근세 · 근대 · 현대` 칩 |
| 시대 전환 | 스크롤하면 배경색이 다음 시대로 서서히 넘어가고, 헤더의 시대 칩도 따라 바뀐다 |
| 낮 ↔ 야경 | 오른쪽 위 `☀ 낮` / `☾ 야경` 버튼 |
| 가로로 더 보기 | 열이 많으면 표를 좌우로 민다. **모든 시대의 표가 같이 움직이고**, 연도 축은 붙어 있다 |
| 상세 보기 | 사건 이름을 누르면 아래에서 시트가 올라온다 — 기간·설명·연대 주석·출처·태그 |
| 같은 해 여러 사건 | 셀에 두 건까지 보이고, `월·일로 보기` / `N건 모두 보기` 칩으로 팝업을 연다 |
| 닫기 | `Esc` 또는 바깥 영역 |

**표가 만들어지는 규칙**

- 행 = `start_year` 기준. 그 시대에 **실제로 사건이 있는 연도만** 행이 된다.
- 열 = 그 시대에 **실제로 사건이 있는 카테고리만**. (선사는 3열, 고대 이후는 11열)
- 연도 아래 `– 종료연도` 는 그 해에 시작한 사건들의 `end_year` 중 가장 늦은 값.
- 카테고리를 끄면 그 사건이 빠지고, 남는 사건이 없는 행·열은 사라진다.
- 시대 전체가 비면 표 대신 한 줄 안내가 나온다.

### 카테고리 탭

`전체 선택` / `전체 해제` 와 트랙별(왕조·국가사 / 주제사) 카드 목록. 끈 카테고리는 연표에서 사라진다.

### 검색 탭

이름·설명·태그·지역을 부분 일치로 찾는다(`/` 키로 바로 이동). 추천어 칩으로 질의를 넣을 수 있고,
결과 카드를 누르면 상세 시트가 열린다.

### 정보 탭

내용 신뢰도 고지, 데이터·소프트웨어 라이선스, 개인정보 고지.

## 6. 배포

### 6-1. GitHub Pages (웹)

빌드 과정이 없으므로 저장소를 그대로 올리면 됩니다.

1. `npm run build` 로 `data/bundle.js` 를 최신화하고 커밋합니다.
2. GitHub 저장소 → **Settings → Pages → Source: Deploy from a branch**
3. 브랜치(예: `main`)와 폴더 `/ (root)` 를 선택하면 `https://<계정>.github.io/<저장소>/` 로 열립니다.

모든 경로가 상대 경로라서 하위 경로 배포에서도 그대로 동작합니다.

### 6-2. PWA (앱스토어 없이 "홈 화면에 추가")

`manifest.webmanifest` 와 `sw.js` 가 이미 들어 있습니다. **https 또는 localhost** 로 열면
서비스워커가 등록되어 오프라인에서도 동작하고, 모바일 브라우저에서 "홈 화면에 추가"를 하면
주소창 없는 앱처럼 실행됩니다.

서비스워커는 **네트워크 우선**으로 동작합니다. 온라인이면 항상 서버의 최신 파일을 쓰고,
네트워크가 없거나 느릴 때만(4초) 캐시 사본으로 대체합니다. 배포 때마다 `npm run build` 로
버전 스탬프가 바뀌므로 새 코드가 곧바로 반영됩니다.

> **예전 버전이 계속 보인다면** (캐시 우선이던 옛 서비스워커가 남아 있는 경우)
> 주소 뒤에 `?v=2` 를 붙여 한 번 열거나, 새로고침을 두 번 하세요.
> 그래도 그대로면 개발자도구 → Application → Service workers → **Unregister** 후 새로고침.
> 지금 어떤 빌드가 떠 있는지는 브라우저 콘솔의 `[app] 빅 히스토리 연표 <해시>` 줄로 확인합니다.

### 6-3. Capacitor (iOS / Android 앱스토어)

이 앱은 웹 표준만 쓰고 데이터도 파일 기반이라(브라우저 저장소에 의존하지 않음)
**코드 수정 없이** 네이티브로 감쌀 수 있습니다.

```bash
npm install @capacitor/core @capacitor/cli
npx cap init "빅 히스토리 연표" com.example.bighistory --web-dir=.
npm install @capacitor/ios @capacitor/android
npx cap add ios
npx cap add android
npx cap sync          # 웹 자산을 네이티브 프로젝트로 복사
npx cap open ios      # Xcode  (App Store 제출)
npx cap open android  # Android Studio (Play Store 제출)
```

- `webDir` 를 저장소 루트로 두면 `index.html`·`css/`·`js/`·`data/` 가 그대로 앱 번들에 들어갑니다.
  (`node_modules`, `ios`, `android`, `scripts` 는 `.gitignore`/`.capacitorignore` 로 제외)
- 네이티브 앱에서 글꼴을 외부에서 받지 않으려면 `index.html` 의 Google Fonts 링크를 지우거나,
  글꼴 파일을 앱 번들에 포함하고 `@font-face` 로 바꾸세요(SIL OFL 이라 재배포 가능).
- 네이티브 WebView 는 `capacitor://` 스킴이라 `fetch` 가 동작하므로 JSON 을 직접 읽습니다.
  `data/bundle.js` 는 그대로 두어도 무해합니다.
- 데이터가 바뀔 때마다 앱을 다시 심사받고 싶지 않다면, 나중에 `data/` 만 원격에서
  내려받아 갱신하는 식으로 `js/data.js` 한 파일만 고치면 됩니다.

---

## 7. 알려진 한계

- **시간 축이 비례하지 않습니다**: 표는 사건이 있는 연도만 한 줄씩 쌓으므로, 행 간격이
  실제 시간 길이를 나타내지 않습니다. 시대별 밀도는 배경과 행 수로 느껴지는 정도입니다.
- **삽화가 아직 없습니다**: 시대 배경과 항목 삽화 자리는 CSS 그라데이션과 자리표시자 라벨입니다.
  실제 그림(시대별 가로 이미지 6장, 권장 1600×640)을 넣을 때는 그 그림의 라이선스를
  `THIRD-PARTY-NOTICES.md` 에 추가하세요.
- **월·일 데이터가 거의 없습니다**: `start_md` 를 채운 항목만 셀에 날짜가 표시되고,
  나머지는 팝업에서 `연중` 으로 나옵니다.
- **연도 0**: 기원전 1년과 서기 1년 사이에 0년은 없습니다. 데이터에 `0` 을 넣으면 검증에서 걸립니다.
- **내용 정확도**: 위 [4장](#4-내용의-출처와-신뢰도) 참고. 모든 항목이 출처 미확인 초안입니다.

---

## 8. 라이선스와 상업적 이용

**광고를 붙여 서비스해도 되는 구조입니다.** 근거를 정리해 둡니다.

| 구성요소 | 라이선스 | 상업적 이용 | 의무 |
|---|---|---|---|
| 앱 코드 (`index.html`, `css/`, `js/`, `scripts/`) | MIT — `LICENSE` | 가능 | 저작권 고지 유지 |
| 연표 데이터 (`data/`) | CC BY 4.0 — `data/LICENSE.md` | 가능 | 저작자 표시 |
| 글꼴 Gowun Batang · Dodum | SIL OFL 1.1 | 가능 | 고지 유지(파일 재배포 안 함) |

- **런타임 의존성이 없습니다.** `package.json` 에 dependencies 가 하나도 없어 전이 의존성 위험이 없습니다.
- **외부 요청은 글꼴 하나뿐입니다.** Google Fonts 에서 Gowun Batang · Gowun Dodum 을 불러옵니다.
  분석 도구·추적 스크립트는 없고 쿠키도 쓰지 않습니다.
  `index.html` 의 `fonts.googleapis.com` 링크를 지우면 **외부 요청이 완전히 사라지고**
  시스템 글꼴로 대체됩니다(폴백이 지정되어 있습니다).
- **글꼴 파일을 재배포하지 않습니다.** 링크로 불러올 뿐입니다.
- **이미지가 없습니다.** `icons/` 는 이 저장소에서 직접 만든 것입니다.
- 앱의 **정보 탭**에 위 고지가 모두 들어 있어, 배포만으로 고지 의무가 충족됩니다.
- 자세한 제3자 고지: `THIRD-PARTY-NOTICES.md`

### 데이터를 채울 때의 유일한 위험

상업적 이용에 문제가 생기는 거의 유일한 경로는 **다른 저작물의 문장을 그대로 옮기는 것**입니다.
특히 **위키백과는 CC BY-SA** 라 문장을 옮기면 그 의무가 데이터 전체로 번질 수 있습니다.

- 연도·사건명 같은 **사실은 자유롭게** 가져와도 됩니다(저작권 보호 대상이 아님).
- `description`·`summary` 는 **반드시 직접 쓰세요.**
- 링크는 `sources` 에 확인용으로 적어도 됩니다. `npm run validate` 가 위키 계열 링크를 만나면
  이 규칙을 다시 알려 줍니다.
- 자세한 규칙: `data/LICENSE.md`

### 광고를 붙일 때 추가로 할 일

지금은 쿠키도 외부 전송도 없지만, **광고 스크립트를 넣는 순간 달라집니다.**

1. **개인정보처리방침 페이지**가 필요합니다(광고 네트워크가 쿠키·식별자를 쓰므로).
2. EU·영국 이용자가 대상이면 **동의 배너(CMP)** 가 필요합니다. 한국만 대상이면 방침 고지로 충분한 경우가 많지만,
   광고 네트워크 약관을 확인하세요.
3. **정보 탭**의 개인정보 문구를 실제와 맞게 고쳐야 합니다(광고 네트워크로의 요청이 생깁니다).
4. 서비스워커는 같은 출처 요청만 가로채므로 광고 로딩과 충돌하지 않습니다.
   다만 오프라인일 때 광고 자리는 비어 보입니다.
5. 콘텐츠 심사가 있는 광고 네트워크라면, **데이터를 `reviewed` 로 끌어올린 뒤** 신청하는 편이 유리합니다
   (현재 395개 전부 `draft`·출처 미확인).
